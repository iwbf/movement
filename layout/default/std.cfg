// Made with Meow~ by Риза
// +----------------------
// | github.com/iwbf/movement
// + ------------------------
// | CONTACTS:
// |  - Discord  ::  tied#0339
// |  - Steam    ::  /id/x__z


// Syntax Information
// + ----------------
// | "<" | ">" w/ something in between - replacement place
// + -----------------------------------------------------
// | <n> | "t"  - <n> tick (ticks) (ex. 1t - 1 tick)
// |               where is "<n>" an integer number of ticks
// | <key>      - replace w/ needed key avoiding the brackets
// |               where is "<key>" the CS:GO key name
// + --------------------------------------------------------
// | "_"         as a prefix     - alternative version of an already defined alias
// |                                that uses any actions w/ 1 tick delay
// |                                or an 1 tick delay action itself
// | <i>         as a suffix     - alternative version of an already defined alias
// |                                where is "<i>" an integer number from 1 to +inf
// | "|"         as a delimiter  - collects expressions into a sequence of themself
// | "::"        as a delimiter  - condition under which current expression will be performed
// | "]::"       as a delimiter  - condition under which all expressions will be performed
// | <i> | "::"  as a delimiter  - condition under which an <i> expression will be performed
// |                                where is "<i>" an integer number from 0 to +inf



// === [ INCLUDES ] ===
include__std_def



// CrouchJump
alias cj "bind space +cj"            // bind  SPACE  to  +cj      action
alias cj_fw "bind space +cj_fw"      // bind  SPACE  to  +cj_fw   action
alias cj_sw "bind space +cj_sw"      // bind  SPACE  to  +cj_sw   action

// --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

// LongJump
alias lj_fw "bind space +lj_fw"      // bind  SPACE  to  +lj_fw   action
alias lj_sw "bind space +lj_sw"      // bind  SPACE  to  +lj_sw   action

// --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

// JumpBug
alias jb "bind space +jb"            // bind  SPACE  to  +jb      action
alias jb_fw "bind space +jb_fw"      // bind  SPACE  to  +jb_fw   action
alias jb_sw "bind space +jb_sw"      // bind  SPACE  to  +jb_sw   action

// --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

// MiniJump
alias mj "bind space +mj"            // bind  SPACE  to  +mj      action
alias mj_fw "bind space +mj_fw"      // bind  SPACE  to  +mj_fw   action
alias mj_sw "bind space +mj_sw"      // bind  SPACE  to  +mj_sw   action

// --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

// Alt
alias alt_mj "bind alt +mj"          // bind  ALT    to  +mj      action
alias alt_mj_fw "bind alt +mj_fw"    // bind  ALT    to  +mj_fw   action
alias alt_mj_sw "bind alt +mj_sw"    // bind  ALT    to  +mj_sw   action

alias alt_strafe "bind alt +strafe"  // bind  ALT    to  +strafe  action

// --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

// Nulls
alias bind_nulls "bind w +null_forward; bind s +null_back; bind d +null_moveright; bind a +null_moveleft"       // bind  W S D A  to  +null_forward +null_back +null_moveright +null_moveleft      actions
alias bind_nulls1 "bind w +null1_forward; bind s +null1_back; bind d +null1_moveright; bind a +null1_moveleft"  // bind  W S D A  to  +null1_forward +null1_back +null1_moveright +null1_moveleft  actions
alias unbind_nulls "bind w +forward; bind s +back; bind d +moveright; bind a +moveleft"                         // bind  W S D A  to  +forward +back +moveright +moveleft                          actions
